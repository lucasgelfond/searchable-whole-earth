A light backend that handles queries to Together.ai for embeddings and Turbopuffer for each. 

## Setup

```bash
pnpm install
```

## Commands

```bash
pnpm dev      # Start local dev server on port 3000
pnpm build    # Build with tsup
pnpm lint     # Lint with Biome
```
