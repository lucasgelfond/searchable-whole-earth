SvelteKit that serves as the interface for the archive. Nothing particularly wild here; 

## Setup

```bash
pnpm install
```

## Commands

```bash
pnpm dev      # Start dev server
pnpm build    # Production build
pnpm check    # TypeScript checking
pnpm lint     # Lint with Biome
```
